apiVersion: v1
kind: Service
metadata:
  name: agentforge-service
  labels:
    app: agentforge
    component: api
spec:
  type: LoadBalancer
  selector:
    app: agentforge
    component: api
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: http
  sessionAffinity: None

