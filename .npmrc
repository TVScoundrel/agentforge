# Link workspace packages during development
link-workspace-packages=true

# Save workspace protocol when adding dependencies
save-workspace-protocol=rolling

# Ensure workspace dependencies are resolved during publish
# This is critical for publishing to npm
auto-install-peers=true

