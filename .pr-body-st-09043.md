## Story
- ST-09043 - Tighten Error Reporter Context Contracts

## What Changed
- tightened `ErrorContext`, `AgentError`, and `AgentError.toJSON()` in `packages/core/src/langgraph/observability/errors.ts`
- replaced broad `any` seams with an unknown-first `state` boundary and JSON-safe `metadata`
- added a source-included typecheck regression in `packages/core/src/langgraph/observability/errors.contracts.typecheck.ts`
- recorded story evidence and tracker updates for EP-09 review flow

## Acceptance Criteria Coverage
- `packages/core/src/langgraph/observability/errors.ts` now replaces broad `state`, `metadata`, and `toJSON()` `any` contracts with unknown-first or JSON-safe aliases
- `AgentError`, `createErrorReporter(...)`, wrapped-node reporting, and fallback reporting behavior are preserved
- focused validation covers serialized payload typing, optional state inclusion, wrapped-node propagation, and public type-boundary regressions
- touched files improve the explicit-`any` baseline and the deltas are recorded in the story doc
- story documentation added at `docs/st09043-error-reporter-context-contracts.md`

## Test Strategy
- test-first via standalone typecheck fixture
- first failing gate proved `state` and `metadata` were still too broad by leaving `@ts-expect-error` directives unused
- existing runtime tests already covered serialized payloads and error propagation, so the new automated coverage was added at the public type boundary

## Validation Performed
- `./node_modules/.bin/tsc --noEmit --strict --module NodeNext --moduleResolution NodeNext --target ES2022 --skipLibCheck --types node packages/core/src/langgraph/observability/errors.contracts.typecheck.ts`
- `pnpm test --run packages/core/tests/langgraph/observability/errors.test.ts`
- `pnpm --filter @agentforge/core typecheck`
- `pnpm lint:explicit-any:baseline`
- `pnpm test --run`
- `pnpm lint`
- `git diff --check`

## Status
- ready for review
- full suite and lint completed
- worktree clean after commits
